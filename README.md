# โปรเจกต์: เกมน้ำเต้าปูปลา (Console-Based, Functional Programming, TypeScript)

โปรเจกต์นี้เป็นระบบจำลองการเล่นเกม **"น้ำเต้าปูปลา"** แบบควบคุมผ่านคอนโซล (CLI) ที่ได้รับการพัฒนาภายใต้กรอบแนวคิด **Functional Programming (FP) อย่างเคร่งครัด** โดยใช้ภาษา TypeScript และรันบน Node.js เพื่อจุดประสงค์ในการแยกตรรกะทางธุรกิจ (Core Logic) ออกจากส่วนติดต่อเชื่อมโยงภายนอก (Impure Shell/Effects) อย่างชัดเจน

---

## 1. วิธีติดตั้งและรันโปรเจกต์

โปรเจกต์นี้ได้รับการทดสอบบนสภาพแวดล้อม Node.js โดยสามารถดำเนินการตามขั้นตอนด้านล่างนี้:

### การติดตั้ง Dependencies
เปิด Terminal ในโฟลเดอร์หลักของโปรเจกต์แล้วรันคำสั่ง:
```bash
npm install
```

### การเริ่มเล่นเกม (Start CLI Game)
รันคอมมานด์ไลน์เพื่อเล่นเกมจำลองในสภาวะจริง:
```bash
npm start
```

---

## 2. โครงสร้างโปรเจกต์ (Folder Structure) และเหตุผลทางสถาปัตยกรรม

โปรเจกต์นี้ใช้สถาปัตยกรรม **Single File + Comment Section** โดยรวมซอร์สโค้ดทั้งหมดไว้ในไฟล์เดียวคือ `namtao.ts` เพื่อความสะดวกในการส่งตรวจและรันด้วยคำสั่งเดียว แต่ยังคงแยกส่วน Pure Core Logic และ Impure Shell อย่างชัดเจนด้วย Comment Section ภายในไฟล์:

```text
namtao/
├── namtao.ts             # ซอร์สโค้ดทั้งหมด (Types, Pure Core, Impure Effects, CLI Loop, Bootstrap)
├── package.json          # กำหนดรายละเอียด dependencies และสคริปต์รัน
├── tsconfig.json         # ตั้งค่า Compiler Options ของ TypeScript
└── game.log              # ไฟล์บันทึกประวัติการเล่น (สร้างขึ้นหลังเล่นเกม)
```

### โครงสร้างภายในไฟล์ `namtao.ts` (แบ่งตาม Comment Section)
```text
// Section 0: TYPES & DOMAIN MODELS         — ประเภทข้อมูลทั้งหมด (GameSymbol, DiceResult, Bet, ...)
// Section 1: PURE CORE LOGIC                — pipe/compose, กติกาเกม, ไปป์ไลน์, parseBetInput
// Section 2: IMPURE EFFECTS                 — rollDice, generateRoundBonus, writeLogToFile, promptUser
// Section 3: MAIN GAME LOOP (CLI)           — formatBetMap, formatRoundBonus, gameLoop
// Section 4: BOOTSTRAP                      — main, entry point
```

### ทำไมจึงเลือกรวมเป็นไฟล์เดียว
โปรเจกต์ถูกพัฒนาในช่วงแรกแบบแยกโมดูลหลายไฟล์ (`src/core/`, `src/effects/`, `src/types/`, `src/utils/`) เพื่อฝึกการแยกโครงสร้าง หลังจากพัฒนาครบทุกฟีเจอร์และ Unit Tests ผ่านทั้งหมดแล้ว จึงทำการ Refactor รวมเป็นไฟล์เดียวเพื่อ:
1. **ส่งตรวจง่าย**: อาจารย์สามารถเปิดไฟล์เดียวและอ่านโค้ดตั้งแต่ต้นจนจบได้ทันที
2. **รันง่าย**: ใช้คำสั่ง `npm start` เพียงคำสั่งเดียว ไม่ต้องกังวลเรื่อง path ของโมดูล
3. **ยังคงแยก Pure/Impure ชัดเจน**: แม้จะอยู่ในไฟล์เดียว แต่ยังคงแยกส่วน Pure Core และ Impure Shell ด้วย Comment Section อย่างเคร่งครัด ทุกฟังก์ชันยังมี tag `// PURE` หรือ `// IMPURE` กำกับครบทุกตัว

### หลักการ Functional Core, Imperative Shell ที่ยังคงไว้
1. **Testability**: ฟังก์ชัน Pure ทั้งหมดสามารถทดสอบได้อย่างแม่นยำ 100% โดยไม่ต้อง Mock เพราะไม่มีการอ้างอิงกับเวลา, ระบบสุ่ม หรือตัวแปรภายนอก
2. **Determinism**: งานที่ไม่แน่นอน (เช่น การทอยเต๋า, การสุ่มโบนัส) ถูกแยกไว้ใน Section 2 (Impure Effects) และผลลัพธ์ถูกฉีด (Inject) เข้าสู่ Pure Pipeline เสมอ
3. **Decoupling**: โค้ดส่วนคำนวณเงินรางวัลไม่ขึ้นตรงกับเครื่องมือรับค่าทางหน้าจอ สามารถนำไปใช้ซ้ำบนแพลตฟอร์มอื่นได้ง่าย

---

## 3. แนวคิด Functional Programming ที่นำมาใช้

### 3.1 Pure Function & Tagging
ทุกฟังก์ชันในโปรเจกต์จะต้องมีการประกาศคุณลักษณะอย่างชัดเจนผ่านแท็กคำอธิบายเหนือลายเซ็นต์ฟังก์ชัน:
- `// PURE` สำหรับฟังก์ชันที่ไม่มี Side Effect และทำงานแบบ Deterministic
- `// IMPURE` สำหรับฟังก์ชันที่ส่งผลกระทบต่อภายนอก อ่านสถานะเครื่อง หรือมีความสุ่ม

ตัวอย่าง:
```typescript
// PURE
export function calculateBasePayout(betAmount: number, matchCount: number): number {
  return matchCount > 0 ? betAmount * (matchCount + 1) : 0;
}
```

### 3.2 Immutability
เรากำหนดโครงสร้างข้อมูลเป็น `readonly` เสมอ เพื่อป้องกันการแก้ไขข้อมูลเดิมโดยตรง (Mutation) แต่จะใช้วิธีสร้างและส่งคืน State ชุดใหม่แทน:
```typescript
export type StreakState = {
  readonly consecutiveWins: number;
  readonly bonusTimeActive: boolean;
  readonly bonusTimeMultiplier: number;
};
```

### 3.3 Function Composition (`pipe` / `compose`)
เราได้เขียนฟังก์ชันจัดเรียงไปป์ไลน์แบบกำหนดเองขึ้นมาใน Section 1 ของ [`namtao.ts`](file:///d:/work/Codex/namtao/namtao.ts) เพื่อร้อยเรียงข้อมูลแบบเป็นลำดับ:
```typescript
// PURE
export function pipe(value: any, ...fns: Function[]): any {
  return fns.reduce((acc, fn) => fn(acc), value);
}
```

ฟังก์ชันไปป์ไลน์จะร้อยเรียงขั้นตอนการคำนวณผลลัพธ์ของรอบ ดังนี้:
```typescript
// PURE
export function runRoundPipeline(input: RoundInput): RoundResult {
  return pipe(
    initRoundContext(input),
    matchResultStep,
    calculateBasePayoutStep,
    resolveMultiplierSourceStep,
    applyFinalMultiplierStep,
    updateStreakStateStep,
    updateStateStep
  );
}
```

### 3.4 Dependency Injection ของ Side Effects
แทนที่จะเรียกใช้ `Math.random()` ภายใน Core Logic โดยตรง เราได้แยกมันไว้ใน Section 2 (Impure Effects) ของ `namtao.ts` แล้วนำผลลัพธ์ที่ได้ **ฉีด (Inject)** เข้าสู่ไปป์ไลน์หลักผ่าน Arguments แทน:
```typescript
// ใน CLI loop (Impure Shell)
const dice = rollDice(); // เรียกฟังก์ชันสุ่ม (Impure)
const result = runRoundPipeline({
  dice,                  // ฉีดผลการสุ่มเข้าไปยัง Pure Core Logic
  ...
});
```

---

## 4. ตารางวิเคราะห์คุณลักษณะของฟังก์ชันในระบบ (PURE / IMPURE Audit)

มีจำนวนฟังก์ชันที่กำหนดในระบบทั้งหมด **24 ฟังก์ชัน** แบ่งออกเป็นดังนี้:

| ลำดับที่ | ชื่อฟังก์ชัน | Section ใน `index.ts` | ประเภท | เหตุผลประกอบการจัดหมวดหมู่ |
| :---: | :--- | :--- | :---: | :--- |
| 1 | `pipe` | Section 1: Pure Core | **PURE** | ทำงานคำนวณลอจิกตามข้อมูลนำเข้าเท่านั้น ไม่มีผลข้างเคียง |
| 2 | `compose` | Section 1: Pure Core | **PURE** | ดำเนินการร้อยเรียงฟังก์ชัน ไม่มีตรรกะที่ไม่สามารถคาดเดาได้ |
| 3 | `getMatchCount` | Section 1: Pure Core | **PURE** | คำนวณผลลัพธ์โดยการนับสัญลักษณ์จากพารามิเตอร์ที่ได้รับ |
| 4 | `calculateBasePayout` | Section 1: Pure Core | **PURE** | คำนวณฐานจ่ายเงินรางวัลตามหลักคณิตศาสตร์ ไม่มี Side effect |
| 5 | `updateStreakState` | Section 1: Pure Core | **PURE** | คำนวณสถานะสตรีคชุดใหม่โดยไม่ปรับแต่งโครงสร้างข้อมูลเดิม |
| 6 | `resolveMultiplierSource` | Section 1: Pure Core | **PURE** | คำนวณคัดแยกแหล่งที่มาของโบนัส ไม่มีปฏิสัมพันธ์กับระบบภายนอก |
| 7 | `applyFinalMultiplier` | Section 1: Pure Core | **PURE** | คำนวณยอดจ่ายสุดท้ายโดยการคูณค่าปกติอย่างเป็นตรรกะตายตัว |
| 8 | `initRoundContext` | Section 1: Pure Core | **PURE** | แปลงโครงสร้างอินพุตเริ่มต้นเป็น Context ของรอบเล่นแบบอ่านอย่างเดียว |
| 9 | `matchResultStep` | Section 1: Pure Core | **PURE** | ขั้นตอนไปป์ไลน์: นับรอบการทอยลูกเต๋า |
| 10 | `calculateBasePayoutStep` | Section 1: Pure Core | **PURE** | ขั้นตอนไปป์ไลน์: คำนวณหาฐานเงินรางวัลของรอบ |
| 11 | `resolveMultiplierSourceStep`| Section 1: Pure Core | **PURE** | ขั้นตอนไปป์ไลน์: ตรวจทานแหล่งโบนัสของทุกการเดิมพัน |
| 12 | `applyFinalMultiplierStep` | Section 1: Pure Core | **PURE** | ขั้นตอนไปป์ไลน์: ประยุกต์ตัวคูณสุดท้ายเข้ากับผลประโยชน์ที่ได้ |
| 13 | `updateStreakStateStep` | Section 1: Pure Core | **PURE** | ขั้นตอนไปป์ไลน์: อัปเดตตรรกะสตรีคแพ้/ชนะ |
| 14 | `updateStateStep` | Section 1: Pure Core | **PURE** | ขั้นตอนไปป์ไลน์: สร้างอ็อบเจกต์ผลลัพธ์ของรอบและเปลี่ยนยอดเงิน |
| 15 | `runRoundPipeline` | Section 1: Pure Core | **PURE** | สังเคราะห์และประกอบลำดับขั้น Pure functions เข้าด้วยกัน |
| 16 | `parseBetInput` | Section 1: Pure Core | **PURE** | ตรวจสอบและแปลงข้อความนำเข้าเป็นพจนานุกรมการเดิมพัน |
| 17 | `formatBetMap` | Section 3: CLI | **PURE** | จัดระเบียบ Bet Map ให้อยู่ในข้อความพร้อมแสดงผลบน Console |
| 18 | `formatRoundBonus` | Section 3: CLI | **PURE** | จัดรูปแบบรายละเอียดโบนัสสุ่มประจำรอบเป็นข้อความพร้อมแสดงผล |
| 19 | `formatNumber` | Section 3: CLI | **PURE** | จัดรูปแบบตัวเลขให้แสดงจุลภาคคั่นหลักพัน เช่น 1,000 |
| 20 | `formatOutcomeMessage` | Section 1: Pure Core | **PURE** | ประกอบข้อความผลลัพธ์การเล่นรอบนั้นพร้อมสัญลักษณ์หน้าตาตอบรับ |
| 21 | `rollDice` | Section 2: Impure Effects | **IMPURE** | มีความไม่แน่นอน (Non-deterministic) จากการสุ่ม `Math.random` |
| 22 | `generateRoundBonus` | Section 2: Impure Effects | **IMPURE** | สุ่มคัดเลือกและกำหนดตัวคูณโบนัสประจำรอบผ่านกลไกความสุ่ม |
| 23 | `writeLogToFile` | Section 2: Impure Effects | **IMPURE** | มีการดัดแปลงสถานะภายนอกระบบโดยการเขียนไฟล์ log ลงบนฮาร์ดดิสก์ |
| 24 | `promptUser` | Section 2: Impure Effects | **IMPURE** | ควบคุมรับค่าและประสานงานผ่านอินพุตและเอาต์พุต (Stdin/Stdout) |
| 25 | `getRandomFace` | Section 2: Impure Effects | **IMPURE** | สุ่มสัญลักษณ์ข้อความหน้าตา (Kaomoji) จากรายการผ่านความสุ่ม |
| 26 | `gameLoop` | Section 3: CLI | **IMPURE** | ควบคุม Game loop ดำเนินการด้าน I/O และรันฟังก์ชันสุ่มสภาวะ |
| 27 | `main` | Section 4: Bootstrap | **IMPURE** | จุดเริ่มต้นเริ่มเกมที่มีการเขียนข้อความและเปิดฟังก์ชัน I/O |

---

## 5. สัดส่วนและผลลัพธ์การวัดผล Pure Functions

โปรเจกต์นี้ผ่านการวิเคราะห์ลอจิกความถูกต้องอย่างเข้มข้นระหว่างการพัฒนา โดยมีผลลัพธ์การตรวจสอบดังนี้:

* **การทดสอบความถูกต้อง (Unit Test)**: ผ่านการทดสอบด้วย Jest ครบ **14/14 test cases** ครอบคลุมทั้งระบบโบนัสสุ่ม, การคำนวณเงินรางวัลสลับซับซ้อน, และการไต่ระดับ Streak ของ Bonus Time แบบอัตโนมัติ
* **สัดส่วน Pure Functions**: มีปริมาณฟังก์ชัน Pure สูงถึง **74.07%** (ผ่านเกณฑ์ขั้นต่ำ >70% ที่กำหนด) โดยคำนวณจากสัดส่วนดังนี้:
  * จำนวนฟังก์ชัน PURE: 20 ฟังก์ชัน
  * จำนวนฟังก์ชัน IMPURE: 7 ฟังก์ชัน
  * รวมฟังก์ชันทั้งหมดในไฟล์ `namtao.ts`: 27 ฟังก์ชัน (ไม่นับรวมฟังก์ชันช่วยเหลือภายใน)

---

## 6. กติกาและระบบพิเศษของเกม

### 6.1 กติกาพื้นฐาน
- สัญลักษณ์ในลูกเต๋ามี 6 หน้า ได้แก่: **น้ำเต้า, ปู, ปลา, ไก่, เสือ, กุ้ง**
- ทอยลูกเต๋า 3 ลูกพร้อมกัน
- ผู้เล่นชนะในรอบนั้น หากมีอย่างน้อยหนึ่งช่องที่แทงแล้วตรงกับผลเต๋า
- อัตราจ่ายปกติ: ตรง 1 ลูก ได้ x1 ของยอดแทง, ตรง 2 ลูก ได้ x2, ตรง 3 ลูก ได้ x3 (พร้อมคืนทุนสำหรับช่องที่ถูก)
- หากไม่ถูกช่องใด ๆ เลย จะเสียเงินเดิมพันในช่องนั้นทั้งหมด

### 6.2 ระบบที่ 1: Random Round Bonus (โบนัสสุ่มประจำรอบ)
- ทำงานเฉพาะตอนที่ผู้เล่น **ไม่ได้อยู่ในช่วง Bonus Time** เท่านั้น
- ก่อนเริ่มเดิมพัน ระบบจะสุ่มสัญลักษณ์ 0 - 3 ตัวเพื่อมอบโบนัสตัวคูณพิเศษ โดยสุ่มตัวคูณจาก Pool ที่กำหนด: **x2, x4, x6, x8, x10** เท่านั้น
- ตัวคูณนี้จะคูณเข้ากับเงินรางวัลปกติ (รวมทุน) ของสัญลักษณ์ที่ถูกเดิมพันเท่านั้น

### 6.3 ระบบที่ 2: Bonus Time (โบนัสสตรีคชนะติดต่อกัน)
- หากผู้เล่น **ชนะ** ติดต่อกันครบ 3 ตา (มีช่องที่แทงถูกอย่างน้อย 1 ช่อง 3 รอบติดกัน) จะเข้าสู่ **Bonus Time** ในรอบถัดไป
- เมื่ออยู่ใน Bonus Time:
  - ระบบจะ**ไม่มีการสุ่มโบนัสประจำรอบ (Random Round Bonus)** (ห้ามใช้ระบบทับซ้อนกัน)
  - ทุกช่องที่ผู้เล่นแทงถูกในตานั้นจะได้รับการคูณเพิ่ม **x2 ทั้งกระดาน** โดยอัตโนมัติ
  - หากเล่นชนะต่อเนื่อง ตัวคูณทวีคูณจะขยับขึ้นทีละรอบ: **x2 → x3 → x4 → x5 → x6** (สูงสุดและคงที่ที่ x6)
  - หาก **แพ้** เพียงครั้งเดียว โหมด Bonus Time จะยกเลิกทันที ยอดสตรีคกลับเป็น 0 และกลับสู่การเล่นโหมดปกติ

---

## 7. ตัวอย่างการเล่นเกมจริง (Gameplay Simulation)

ต่อไปนี้เป็นตัวอย่างสถานการณ์จำลอง 3 รอบของการเล่น:

### รอบที่ 1: โหมดปกติ ได้รับโบนัสสุ่มประจำรอบ
- **สถานะเริ่มต้น**: ยอดเงินคงเหลือ = 1,000 บาท, ชนะติดต่อกัน = 0, ไม่ได้อยู่ใน Bonus Time
- **โบนัสสุ่มประจำรอบที่เกิดขึ้น**: `เสือ x4` และ `กุ้ง x10`
- **ผู้เล่นแทง**: `เสือ 100 บาท` และ `ปลา 100 บาท` (รวมวางเดิมพัน 200 บาท)
- **ผลการทอยเต๋า**: `[เสือ] [ปลา] [ไก่]`
- **ผลลัพธ์**:
  - `เสือ`: แทงถูก 1 ลูก -> ฐานจ่ายปกติ = 200 (ชนะ 100 + คืนทุน 100) -> ได้รับโบนัส `เสือ x4` -> ได้รับคืนจริง = 200 * 4 = 800 บาท
  - `ปลา`: แทงถูก 1 ลูก -> ฐานจ่ายปกติ = 200 -> ไม่มีโบนัสประจำรอบ -> ได้รับคืนจริง = 200 บาท
  - **สรุป**: แทงรวม 200 บาท ได้รับคืนรวม 1,000 บาท (กำไรสุทธิ +800 บาท)
  - **สถานะปลายรอบ**: ยอดเงินคงเหลือ = 1,600 บาท, สตรีคชนะ = 1

### รอบที่ 2: ปลดล็อกเข้าสู่โหมด Bonus Time
- **สถานะเริ่มต้น**: ยอดเงินคงเหลือ = 1,600 บาท, ชนะติดต่อกัน = 2 (ชนะมาจากรอบก่อนหน้า)
- **ผู้เล่นแทง**: `กุ้ง 100 บาท` (รวมวางเดิมพัน 100 บาท)
- **ผลการทอยเต๋า**: `[กุ้ง] [ไก่] [ปู]` (ชนะรอบนี้)
- **สถานะปลายรอบ**: ชนะติดต่อกันสะสมเป็น 3 ตา -> ปลดล็อก **Bonus Time** ในตาถัดไป
- **ยอดเงินใหม่**: 1,700 บาท

### รอบที่ 3: เล่นอยู่ในช่วง Bonus Time
- **สถานะเริ่มต้น**: ยอดเงินคงเหลือ = 1,700 บาท, โหมด Bonus Time ทำงาน (ตัวคูณทั้งกระดาน x2)
- **โบนัสสุ่มประจำรอบ**: *ไม่มีการสร้างโบนัสสุ่มสัญลักษณ์* เนื่องจากกติกาห้ามซ้อนกัน
- **ผู้เล่นแทง**: `ปลา 100 บาท` (รวมวางเดิมพัน 100 บาท)
- **ผลการทอยเต๋า**: `[ปลา] [ปลา] [น้ำเต้า]`
- **ผลลัพธ์**:
  - `ปลา`: แทงถูก 2 ลูก -> ฐานจ่ายปกติ = 300 (ชนะ 200 + คืนทุน 100) -> ได้รับผลคูณทั้งกระดาน x2 -> ได้รับเงินคืนจริง = 300 * 2 = 600 บาท
  - **สรุป**: แทงรวม 100 บาท ได้รับคืนรวม 600 บาท (กำไรสุทธิ +500 บาท)
  - **สถานะปลายรอบ**: ยอดเงินคงเหลือ = 2,200 บาท, สตรีคชนะ = 4, Bonus Time ขยับระดับตัวคูณขึ้นเป็น x3 สำหรับรอบต่อไป

---

## 8. แผนภูมิขั้นตอนการทำงานหลัก (Game Pipeline Diagram)

ไปป์ไลน์แสดงขั้นตอนการไหลเวียนของสถานะและการคำนวณภายในรอบการเล่นเกม:

```text
[ CLI Impure Shell / Effects ]
              │
              ▼
1. สุ่มโบนัสประจำรอบ (generateRoundBonus) หรือ ตรวจพบ Bonus Time
              │
              ▼
2. รับข้อมูลการเดิมพันจากหน้าคอนโซล (promptUser)
              │
              ▼
3. ตรวจสอบความถูกต้องและแปลงอินพุต (parseBetInput)
              │
              ▼
4. ทอยลูกเต๋า 3 ลูก (rollDice)
              │
              ▼
   ──────────────────────────────────────────────────────────
   [ Pure Pipeline - runRoundPipeline (fp.pipe) ]
   
   5. [ initRoundContext ]
      สร้างแบบจำลองสภาวะแวดล้อมตั้งต้นสำหรับใช้วิเคราะห์ผล
              │
              ▼
   6. [ matchResultStep ]
      คำนวณจำนวนหน้าเต๋าที่จับคู่ตรงกับช่องเดิมพันของผู้เล่น
              │
              ▼
   7. [ calculateBasePayoutStep ]
      คำนวณเงินรางวัลพื้นฐานแบบปกติของแต่ละหน้า (bet * (matchCount + 1))
              │
              ▼
   8. [ resolveMultiplierSourceStep ]
      ประเมินว่าตัวคูณของสัญลักษณ์มาจาก Bonus Time หรือ Random Round Bonus
              │
              ▼
   9. [ applyFinalMultiplierStep ]
      นำตัวคูณไปทวีคูณเข้ากับฐานการเงินรางวัลเดิม
              │
              ▼
  10. [ updateStreakStateStep ]
      ประเมินผล แพ้/ชนะ ของรอบ แล้วทำการคิดค้นระดับ Streak ใหม่
              │
              ▼
  11. [ updateStateStep ]
      หักล้างยอดคงเหลือสะสม และสังเคราะห์ข้อมูลประวัติเป็น RoundResult
   ──────────────────────────────────────────────────────────
              │
              ▼
12. บันทึกข้อมูลและบันทึกประวัติการเล่น (writeLogToFile)
              │
              ▼
13. แสดงผลหน้าจอพร้อมเริ่มรอบการทำงานถัดไป (gameLoop - Recurse)
```

---

## 9. อธิบายโค้ดโดยละเอียดทีละบรรทัด (Line-by-Line Code Explanation)

เพื่อให้ผู้อ่านที่ไม่มีพื้นฐานการเขียนโปรแกรม (Non-programmer) สามารถทำความเข้าใจระบบจำลองน้ำเต้าปูปลาชุดนี้ได้โดยง่าย ส่วนนี้จะชำแหละและอธิบายการทำงานในแต่ละช่วงของไฟล์ [`namtao.ts`](file:///d:/work/Codex/namtao/namtao.ts) อย่างละเอียด พร้อมระบุคุณลักษณะความเป็น Pure / Impure ของแต่ละฟังก์ชันอย่างชัดเจน

---

### 9.1 การตั้งต้นระบบและจัดทำประเภทข้อมูล (Section 0: TYPES & DOMAIN MODELS)

เป็นส่วนการนิยาม "แบบแปลน" หรือข้อกำหนดเบื้องต้นว่าสิ่งของต่างๆ ในเกมจะมีหน้าตาและข้อจำกัดอย่างไร (ไม่ส่งผลกระทบใดๆ จึงมีสถานะเป็นความบริสุทธิ์เชิงคำอธิบายทางคณิตศาสตร์)

*   **การนำเข้า Library หรือคลังเครื่องมือ (บรรทัดที่ 1-3)**
    *   `import * as fs from 'fs';` : ดึงเอาเครื่องมือจัดการไฟล์ของ Node.js เพื่อนำมาเขียนประวัติการเล่นเกมลงในเครื่องคอมพิวเตอร์ของคุณ
    *   `import * as path from 'path';` : นำเข้าเครื่องมือประมวลผลเส้นทางที่อยู่ไฟล์ (Path) ช่วยหาตำแหน่งของโฟลเดอร์รันเกม
    *   `import * as readline from 'readline';` : ดึงกล่องรับข้อความพิมพ์เข้า-ออก เพื่อให้เครื่องอ่านสิ่งที่ผู้ใช้พิมพ์จากคีย์บอร์ดได้
*   **โครงสร้างข้อมูลที่ใช้งานในเกม (บรรทัดที่ 9-45)**
    *   `GameSymbol` : กำหนดสัญลักษณ์ที่อนุญาตให้มีในเกม มีเพียง 6 ตัวคือ 'น้ำเต้า', 'ปู', 'ปลา', 'ไก่', 'เสือ', 'กุ้ง'
    *   `DiceResult` : ผลการทอยเต๋าที่ถูกล็อคไว้ให้แสดงสัญลักษณ์ 3 ลูก และมีคำว่า `readonly` ข้างหน้าเพื่อบ่งบอกว่า **"ห้ามแอบแก้ไขผลเต๋าเมื่อทอยเสร็จเด็ดขาด"**
    *   `Bet` : แผนผังการแทงของผู้เล่น จับคู่กันระหว่าง "สัญลักษณ์ที่จะแทง" และ "เงินเดิมพัน" (เช่น แทงปลา 100 บาท)
    *   `RoundBonus` : แผนผังตัวคูณสุ่มที่จะบอกว่า สัญลักษณ์ไหนจะได้รับการคูณเงินรางวัลเพิ่มในแต่ละตานั้นๆ
    *   `StreakState` : ประวัติตาที่ผู้เล่นเล่นชนะติดต่อกัน เพื่อเอาไว้ตัดสินใจเปิด/ปิด **"Bonus Time"**
        *   `consecutiveWins` : จำนวนครั้งที่ผู้เล่นสามารถเล่นชนะกี่ตาติดต่อกันแล้ว
        *   `bonusTimeActive` : โหมด Bonus Time กำลังทำงานอยู่จริงหรือไม่ (จริง / เท็จ)
        *   `bonusTimeMultiplier` : ตัวเลขตัวคูณทวีคูณที่จะทวีเพิ่มตั้งแต่ x2 ไปจนสูงสุดที่ x6
    *   `RoundResult` : บันทึกทุกสิ่งทุกอย่างที่เกิดขึ้นในรอบนั้นๆ เช่น ออกเต๋าอะไร ยอดเงินก่อนเล่นและหลังเล่นเป็นอย่างไร มีโบนัสหรือไม่ เพื่อทำประวัติสรุปการเล่น
    *   `PlayHistory` : รายการประวัติที่คอยรวบรวมใบเสร็จสรุปผลการเล่นจากแต่ละตาเก็บสะสมไปเรื่อยๆ
    *   `GameState` : สถานะปัจจุบันของเกม ณ ตานั้นๆ ได้แก่ ยอดเงินคงเหลือล่าสุดของผู้เล่น, ประวัติการเล่นชนะสะสม และรายการบันทึกประวัติการเล่นที่ผ่านมาทั้งหมด

---

### 9.2 สมองประมวลผลหลักของเกม (Section 1: PURE CORE LOGIC)

ส่วนควบคุมกติกาของน้ำเต้าปูปลาที่ถูกคิดค้นขึ้นมาเป็น **Pure Function** (ไม่มี Side Effect ทำงานอย่างตรงไปตรงมา ยื่นค่าอะไรเข้าไป ย่อมได้ค่าผลลัพธ์ตัวเดิมส่งคืนกลับมาเสมอ และไม่ยุ่งเกี่ยวกับการเชื่อมต่อใดๆ ภายนอกเลย)

*   **ฟังก์ชัน `pipe` และ `compose` (บรรทัดที่ 53-70) — [PURE]**
    *   ทำหน้าที่เหมือน **"สายพานในโรงงานประกอบรถยนต์"** ที่คอยรับรถดิบเข้ามา แล้วส่งผ่านเครื่องจักรชิ้นที่ 1, 2, 3 เพื่อพ่นสี ติดประตู และส่งออกเป็นรถที่สมบูรณ์แบบโดยไม่ต้องใช้โค้ดวนลูปที่รกรุงรัง
*   **ฟังก์ชัน `getMatchCount` (บรรทัดที่ 73-75) — [PURE]**
    *   `dice.filter((d) => d === symbol).length;` : ทำหน้าที่กรองเช็คดูว่าผลลัพธ์ลูกเต๋าทั้ง 3 ลูก มีลูกไหนที่สัญลักษณ์ตรงกับที่เราแทงไว้บ้าง แล้วนับจำนวนที่ถูกนั้นส่งคืนกลับไป (คืนค่าเป็น 0 ถึง 3)
*   **ฟังก์ชัน `calculateBasePayout` (บรรทัดที่ 78-80) — [PURE]**
    *   `matchCount > 0 ? betAmount * (matchCount + 1) : 0;` : หากผู้เล่นทอยเต๋าถูกตามเงื่อนไข จะได้เงินรางวัลตามอัตรา `เงินแทง * (จำนวนลูกเต๋าที่ทอยถูก + 1)` โดยการบวก 1 นี้คือการได้รับเงินทุนเดิมพันของเราคืนกลับมาด้วยนั่นเอง หากทอยไม่ถูกเลย ยอดเงินรางวัลฐานจะเท่ากับ 0
*   **ฟังก์ชัน `updateStreakState` (บรรทัดที่ 83-122) — [PURE]**
    *   ตรรกะคำนวณการสะสมสตรีคชนะติดกัน:
        *   **หากยังไม่ได้อยู่ใน Bonus Time** : ถ้าตานี้เล่นชนะ จำนวนสะสม `consecutiveWins` จะบวกเพิ่ม 1 และเมื่อสะสมครบ 3 ตา ระบบจะทำการเปิดโหมด Bonus Time และตั้งค่าตัวคูณของตาถัดไปให้เป็น x2 ทันที แต่ถ้าตานี้เล่นแพ้ ประวัติสะสมจะถูกตีกลับไปเป็น 0
        *   **หากอยู่ใน Bonus Time อยู่แล้ว** : ถ้าเล่นชนะต่อไปเรื่อยๆ ระบบจะบวกระดับตัวคูณสะสมขึ้นตาทละ 1 เท่า (เช่น x2 -> x3 -> x4...) โดยถูกจำกัดเพดานสูงสุดไว้ที่ x6 แต่ถ้าเล่นแพ้แม้เพียงครั้งเดียว โหมดโบนัสไทม์จะถูกยกเลิกทันที ยอดสตรีคกลับเป็น 0
*   **ฟังก์ชัน `resolveMultiplierSource` (บรรทัดที่ 125-159) — [PURE]**
    *   ตรวจสอบเพื่อคัดเลือกตัวคูณรางวัลในตานั้น:
        *   ถ้าโหมด Bonus Time กำลังทำงานอยู่ จะเลือกรับสิทธิ์คูณเพิ่มจากระดับตัวคูณของ Bonus Time โดยตรง
        *   แต่ถ้าผู้เล่นไม่ได้อยู่ใน Bonus Time จะตรวจสอบว่าสัญลักษณ์ที่แทงถูกในตานี้มีโบนัสสุ่มประจำรอบติดตั้งอยู่หรือไม่ หากมีก็จะได้คูณตามตัวคูณนั้น
        *   หากไม่มีเงื่อนไขใดๆ เข้าเกณฑ์เลย จะได้ตัวคูณธรรมดาคือ 1 เท่า (ได้ตามผลปกติ)
*   **ฟังก์ชัน `applyFinalMultiplier` (บรรทัดที่ 162-170) — [PURE]**
    *   `basePayout * resolvedMultiplier.multiplier;` : นำยอดเงินรางวัลฐานปกติที่ได้ มาคูณกับตัวคูณโบนัสสุดท้ายที่วิเคราะห์ได้จากขั้นตอนก่อนหน้า
*   **ฟังก์ชัน `initRoundContext` (บรรทัดที่ 202-220) — [PURE]**
    *   สร้างร่างเตรียมเก็บข้อมูลกระดาษบันทึกของรอบนั้นๆ โดยการคำนวณหาผลรวมของเงินเดิมพันทั้งหมดที่ผู้เล่นวางแทงในทุกสัญลักษณ์
*   **ฟังก์ชัน `matchResultStep` ถึง `updateStateStep` (บรรทัดที่ 223-353) — [PURE]**
    *   เป็นขั้นตอนย่อยๆ บนสายพาน (`pipe`) ที่ใช้เปลี่ยนรูปข้อมูลประวัติของรอบไปทีละนิด เช่น นับผลเต๋า -> คำนวณรางวัลฐาน -> ดึงโบนัสสุ่ม -> คิดระดับเงินรางวัลสุดท้าย -> อัปเดตสตรีคการชนะ -> ตลอดจนหาผลรวมเงินแพ้ชนะและเงินในกระเป๋าใหม่ของผู้เล่น
*   **ฟังก์ชัน `runRoundPipeline` (บรรทัดที่ 356-366) — [PURE]**
    *   มัดรวมขั้นตอนสายพานประมวลผลทั้งหมดข้างบนไว้ด้วยกัน เพื่อให้เรียกใช้ได้ง่ายๆ ด้วยคำสั่งเดียว
*   **ฟังก์ชัน `parseBetInput` (บรรทัดที่ 369-401) — [PURE]**
    *   ทำหน้าที่เป็นเครื่องตรวจคนเข้าเมืองด้านคำพูด: รับคำสั่งข้อความที่ผู้ใช้พิมพ์เข้ามา (เช่น "ปลา 100") มาทำการหั่นเพื่อเช็คความถูกต้อง
    *   ตรวจว่าใส่สัญลักษณ์ตรงตามกติกาหรือไม่ (น้ำเต้า ปู ปลา ไก่ เสือ กุ้ง) และตัวเงินจะต้องเป็นตัวเลขทางบวกที่มากกว่า 0 เสมอ
*   **ฟังก์ชัน `formatOutcomeMessage` (บรรทัดที่ 404-408) — [PURE]**
    *   เรียงร้อยสายอักขระเพื่อประดับประดาข้อความว่า "คุณชนะ" หรือ "คุณแพ้" ยอดเงินเท่าไหร่ พร้อมการต่ออีโมจิปิดท้าย

---

### 9.3 ระบบสุ่มและระบบติดต่ออุปกรณ์บันทึก (Section 2: IMPURE EFFECTS)

ส่วนติดต่อปัจจัยภายนอกที่ไม่แน่นอนหรือส่งผลกระทบออกนอกโปรแกรม (**Impure Function** มี Side Effect หรือไม่คงเส้นคงวาในการสุ่มข้อมูล)

*   **ฟังก์ชัน `rollDice` (บรรทัดที่ 420-423) — [IMPURE]**
    *   `Math.random()` : เป็นฟังก์ชันสุ่มเต๋าโดยอาศัยระบบสุ่มเลขของคอมพิวเตอร์ ทำให้อินพุตที่ส่งเข้าไปจะได้คำตอบต่างกันทุกครั้งที่ทอยเต๋าใหม่ (ไม่สามารถพยากรณ์ได้)
*   **ฟังก์ชัน `generateRoundBonus` (บรรทัดที่ 428-441) — [IMPURE]**
    *   สุ่มจับฉลากว่ารอบนั้นๆ จะมีสัญลักษณ์กี่ตัวที่ได้โบนัส และสุ่มหยิบตัวคูณพิเศษขึ้นมา ซึ่งมีความสุ่มไม่แน่นอนเช่นกัน
*   **ฟังก์ชัน `writeLogToFile` (บรรทัดที่ 445-448) — [IMPURE]**
    *   `fs.appendFileSync(...)` : ทำการเปิดดิสก์ฮาร์ดดิสก์ของคุณเพื่อเขียนบรรทัดประวัติลงไปในไฟล์ `game.log` (สร้างผลกระทบนอกระบบของโปรแกรม)
*   **ฟังก์ชัน `promptUser` (บรรทัดที่ 452-464) — [IMPURE]**
    *   `rl.question(...)` : ทำการเปิดรับข้อมูลจากแป้นพิมพ์ที่ผู้ใช้งานพิมพ์เข้ามาระหว่างเล่นเกม ซึ่งจะชะงักการทำงานของโปรแกรมจนกว่าผู้ใช้จะส่งข้อมูลเข้ามา
*   **ฟังก์ชัน `getRandomFace` (บรรทัดที่ 468-470) — [IMPURE]**
    *   สุ่มเลือกรูปหน้า Kaomoji แสดงผล เพื่อเพิ่มอรรถรสในหน้าสรุปผล

---

### 9.4 เครื่องยนต์หน้าจอควบคุมการเล่น (Section 3: MAIN GAME LOOP)

ส่วนสั่งการแสดงผลหน้าจอบนคอนโซล CLI และการดักรับขั้นตอนการทำงานวนซ้ำอย่างเป็นระบบ

*   **ค่าคงที่จัดแต่งความสวยงาม (บรรทัดที่ 478-510)**
    *   มีการประกาศข้อความตกแต่งหัวข้อ เช่น `[WIN]` หรือรูปภาพอารมณ์ใบหน้าญี่ปุ่น (Kaomoji) เอาไว้ใช้งานซ้ำๆ
*   **ฟังก์ชัน `formatBetMap` และ `formatRoundBonus` (บรรทัดที่ 512-529) — [PURE]**
    *   นำแผนผังการแทงเงินและตัวคูณโบนัสสุ่ม มาจัดเรียงข้อความให้อ่านง่าย เช่น "ปู: 100 THB" หรือ "ปลา x4 | ไก่ x10" เพื่อนำไปพ่นออกหน้าจอ
*   **ฟังก์ชัน `formatNumber` (บรรทัดที่ 532-534) — [PURE]**
    *   `n.toLocaleString('en-US')` : เติมเครื่องหมายจุลภาคคั่นระหว่างตัวเลข เช่น `1500` -> `1,500`
*   **ฟังก์ชัน `gameLoop` (บรรทัดที่ 539-720) — [IMPURE]**
    *   **แกนเครื่องยนต์หลักของเกม** ที่จะทำงานซ้ำเรื่อยๆ ในแต่ละตา:
        1.  วาดหน้ากระดานสรุปสถานะการเล่นล่าสุด ยอดเงินคงเหลือ และโบนัสที่ทำงานในตานั้นๆ
        2.  เปิดระบบดักรับค่าจากผู้เล่น เพื่อให้พิมพ์แทงเงินในแต่ละรอบ ตรวจเช็คข้อมูล หากอยากเลิกสามารถพิมพ์ "exit"
        3.  ส่งข้อมูลการวางเดิมพันที่ถูกต้อง ไปประมวลผลจับคู่กับการสุ่มผลเต๋าและสุ่มตัวคูณพิเศษ ผ่านสมอง Core Logic ที่เป็นระเบียบของระบบ
        4.  วาดตารางคำนวณเงินรางวัลในรอบอย่างสวยงาม แสดงสัญลักษณ์ที่ชนะ และตัวคูณที่ได้
        5.  พิมพ์ประวัติสรุปการแพ้ชนะลงสู่ไฟล์บันทึกความจำภายนอก
        6.  เช็คสถานะเงินกระเป๋าผู้เล่น ถ้ากลายเป็น 0 จะเสนอการเติมเงินเพื่อเริ่มตักตวงโชคใหม่ แต่ถ้ามีเงินเหลือจะถามให้กดปุ่ม Enter เพื่อวนซ้ำรันคำสั่งเล่นตาต่อไป

---

### 9.5 ประตูบานแรกเริ่มเปิดรันโปรแกรม (Section 4: BOOTSTRAP)

*   **ฟังก์ชัน `main` (บรรทัดที่ 729-751) — [IMPURE]**
    *   เป็นจุดเช็คอินแรกเมื่อผู้ใช้สั่งเริ่มเกม ทำหน้าที่พิมพ์อธิบายโครงสร้างกติกาน้ำเต้าปูปลาเบื้องต้น ตั้งค่าเริ่มกระเป๋าเงินแรกเข้าที่ 1,000 บาท และส่งไม้ต่อเปิดประตูเข้าสู่ `gameLoop` สู่ความเพลิดเพลินในการเล่นเกม
*   **การดักจับการรันไฟล์โดยตรง (บรรทัดที่ 753-758) — [IMPURE]**
    *   `if (require.main === module)` : คำสั่งตรวจสอบเงื่อนไขว่าไฟล์นี้กำลังถูกรันจาก Node.js โดยตรงหรือเปล่า เพื่อจะไปดึงฟังก์ชัน `main()` ให้ตื่นขึ้นมาทำงานทันที และมี `.catch()` คอยดักจับกรณีเกิดข้อผิดพลาดร้ายแรงของระบบจนไม่สามารถไปต่อได้
โค้ดส่วนนี้คือ เครื่องมือสำหรับร้อยเรียงคำสั่ง (Function Composition) ซึ่งเป็นหัวใจสำคัญของการเขียนโปรแกรมแนว Functional Programming ครับ

เพื่อให้เข้าใจง่ายที่สุด จะขออธิบายแบ่งเป็น 2 ส่วนหลัก ๆ ดังนี้ครับ:

1. pipe (ท่อส่งข้อมูล)
เปรียบเสมือน "สายพานลำเลียงในโรงงาน" ที่เราโยนวัตถุดิบลงไปที่ต้นทาง แล้วข้อมูลจะวิ่งผ่านเครื่องจักร (ฟังก์ชัน) ตัวที่ 1 → 2 → 3 ตามลำดับจนได้ผลลัพธ์สุดท้ายออกมาที่ปลายท่อ

ทำไมต้องมีโค้ดซ้ำ ๆ กันหลายบรรทัด (บรรทัดที่ 53 - 62)?
บรรทัดที่มีข้อความ <A, B, C...> เยอะ ๆ นั้นไม่ใช่โค้ดทำงานจริง แต่เป็นสิ่งที่เรียกว่า "Function Overloading" ในภาษา TypeScript ครับ

หน้าที่ของมัน: คือการบอกคนตรวจโค้ด (Compiler) ให้รู้ว่าประเภทข้อมูล (Data Type) ที่ไหลผ่านแต่ละขั้นตอนมีหน้าตาเป็นอย่างไร เช่น
ถ้าขั้นตอนที่ 1 รับข้อมูลประเภท A แล้วแปลงเป็น B
ขั้นตอนที่ 2 รับ B แล้วแปลงเป็น C
ระบบจะคอยเช็คให้เราทันทีว่าเราส่งข้อมูลต่อกันถูกประเภทหรือไม่ เพื่อไม่ให้เกิดบั๊ก
โค้ดที่ทำงานจริง (บรรทัดที่ 63 - 65):
typescript
export function pipe(value: any, ...fns: Function[]): any {
  return fns.reduce((acc, fn) => fn(acc), value);
}
value: คือข้อมูลตั้งต้นที่เราจะส่งเข้าท่อ
...fns: คือรายการฟังก์ชันทั้งหมดที่เราอยากให้ข้อมูลนี้วิ่งผ่าน
fns.reduce((acc, fn) => fn(acc), value):
reduce คือการยุบรวมข้อมูลทีละขั้น โดยระบบจะเริ่มเอา value ไปส่งให้ฟังก์ชันแรก (fn(acc))
พอนำข้อมูลไปประมวลผลเสร็จ ก็นำผลลัพธ์ที่ได้ไปส่งต่อให้ฟังก์ชันถัดไป วนไปเรื่อย ๆ จนหมดทุกฟังก์ชันและได้ผลลัพธ์สุดท้ายออกมา
ตัวอย่างเปรียบเทียบ:

แบบไม่ใช้ pipe (อ่านจากในไปนอก ยุ่งเหยิง): หั่น(ปอก(ล้าง(แอปเปิ้ล)))
แบบใช้ pipe (อ่านจากซ้ายไปขวา ง่ายมาก): pipe(แอปเปิ้ล, ล้าง, ปอก, หั่น)
2. compose (ฟังก์ชันประกอบร่าง)
เปรียบเสมือน "การรวมร่างเครื่องจักรสองเครื่องให้กลายเป็นเครื่องเดียวกัน" โดยทำงานสวนทางกับ pipe (ทำจากขวาไปซ้าย หรือจากหลังมาหน้าตามหลักคณิตศาสตร์)

typescript
export function compose<A, B, C>(f: (b: B) => C, g: (a: A) => B): (a: A) => C {
  return (x: A) => f(g(x));
}
f และ g คือฟังก์ชันสองตัวที่เราต้องการนำมารวมร่างกัน
return (x: A) => f(g(x)) : เมื่อเราส่งวัตถุดิบ x เข้าไปในเครื่องจักรที่รวมร่างนี้
มันจะเอา x ไปส่งให้เครื่องจักร g ทำงานก่อน ได้ผลลัพธ์เป็น g(x)
จากนั้นค่อยเอาผลลัพธ์นั้นส่งต่อให้เครื่องจักร f ทำงานปิดท้าย ได้ผลสุดท้ายเป็น f(g(x))
ตัวอย่างเปรียบเทียบ:

สมมติให้ฟังก์ชัน g คือการ "สตาร์ทรถ"
สมมติให้ฟังก์ชัน f คือการ "เหยียบคันเร่ง"
ถ้าเรานำมารวมร่างด้วย compose(เหยียบคันเร่ง, สตาร์ทรถ) เราจะได้ฟังก์ชันใหม่ชื่อว่า "ขับรถออกตัว" ซึ่งเมื่อเรากดปุ่มใช้งาน มันจะสตาร์ทรถก่อนแล้วจึงเหยียบคันเร่งให้เราทันทีโดยอัตโนมัติ
